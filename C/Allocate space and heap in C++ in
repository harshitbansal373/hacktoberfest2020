#include<iostream>
using namespace std;
int main(){
   int count;
   size_t heap;

    cout<<"Enter the space to Allocate: ";
    cin>>count;
    cout<<"Enter the Heap: ";
    cin>>heap;

    for (size_t i = 1; i <= count; i++)
    {
        cout<<heap<<" ";
    }
    
}
